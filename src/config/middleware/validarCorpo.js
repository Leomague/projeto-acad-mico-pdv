const joi = require('joi');

const schemaCadastro = 'use o joi para fazer o intermediario de validação do Body cadastro';

const schemaLogin = 'use o joi para fazer o intermediario de validação do Body login';

module.exports = {
  schemaCadastro,
  schemaLogin
}
